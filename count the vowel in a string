#include <stdio.h>

int main() {
	char ch[50],v[]="aeiouAEIOU";
	int count = 0,i,j;
	scanf("%s",&ch);
	for(i=0;ch[i];i++)
	{
	    for(j=0;v[j];j++)
	    {
	        if(ch[i]==v[j])
	        {
	            count++;
	            break;
	        }
	    }
	}
	printf("%d",count);
	return 0;
}

